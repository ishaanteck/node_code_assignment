const SUCCESS_MESSAGES = require("./successMessage.constant");
const ERROR_MESSAGES = require("./errorMessages.constant");

module.exports = Object.freeze({
  SUCCESS_MESSAGES,
  ERROR_MESSAGES,
});